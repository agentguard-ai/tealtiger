/** @type {import('next').NextConfig} */
const nextConfig = {
  // Support both static export and server-rendered modes
  // Uncomment the next line for static export (air-gapped / local use):
  // output: 'export',
  reactStrictMode: true,
};

module.exports = nextConfig;
