/*
 * AUTO-GENERATED from packages/tealtiger-contracts/schemas.
 * Do not edit this file directly. Run npm run generate instead.
 */

/**
 * Highest reversibility class the connector can safely represent for the declared operation surface.
 */
export type ReversibilityClass =
  'read_only' | 'reversible' | 'externally_reversible' | 'irreversible';
/**
 * Semantic contract version. This package supports major version 1 and emits 1.0.0.
 */
export type ContractVersion = string;

/**
 * A connector's explicit capability declaration. Unsupported policy mappings must be reported rather than silently approximated.
 */
export interface TargetCapability {
  /**
   * Stable identifier for the connector implementation.
   */
  connector_id: string;
  /**
   * Whether retrying the same execution identity is safe and returns the prior result.
   */
  idempotent: boolean;
  reversibility_class: ReversibilityClass;
  /**
   * Whether the connector can enforce exact-action approval before execution.
   */
  supports_approval: boolean;
  /**
   * Whether the connector can enforce target-state preconditions.
   */
  supports_precondition: boolean;
  /**
   * Whether the connector can reconcile execution against native target evidence.
   */
  supports_reconciliation: boolean;
  /**
   * Whether the connector exposes an explicit compensation operation.
   */
  supports_compensation: boolean;
  /**
   * How faithfully TealTiger policy semantics map to the target's native controls.
   */
  policy_mapping_fidelity: 'EXACT' | 'CONSERVATIVE' | 'OBSERVE_ONLY' | 'UNSUPPORTED';
  contract_version: ContractVersion;
  [k: string]: unknown;
}
